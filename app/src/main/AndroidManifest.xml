<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:name=".app.SmartSleepApplication"
        android:label="Smart Sleep">
        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.alarm.AlarmActivity"
            android:exported="false" />
        <receiver
            android:name=".app.alarm.AlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".app.alarm.AlarmDismissReceiver"
            android:exported="false" />
        <service
            android:name=".app.transport.PhoneDataLayerListener"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.BIND_LISTENER" />
            </intent-filter>
        </service>
    </application>
</manifest>
